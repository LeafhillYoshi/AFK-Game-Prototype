import { Menu } from './menu';

export class TavernView extends Phaser.Scene {
    constructor() {
        super({ key: 'TavernView' });
    }

    preload() {
    }

    create() {    
    }

    update() {
    // Update logic for the tavern view    
    }
}

export class TavernViewUI extends Phaser.Scene {
    constructor() {
        super({ key: 'TavernViewUI'});
    }

    confirm() {
    }
}